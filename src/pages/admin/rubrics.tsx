import type React from "react"
import { Typography } from "antd"

const { Title } = Typography

export const AdminRubricsPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>Quản lý Rubric chấm điểm</Title>
      <p>Trang quản lý rubric chấm điểm sẽ được phát triển trong phiên bản tiếp theo.</p>
    </div>
  )
}
